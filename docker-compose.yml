version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"

  dashboard:
    build: .
    ports:
      - "3005:3000"
    environment:
      - NODE_ENV=production
      - VITE_API_URL=http://localhost:8000
    depends_on:
      - backend
